{
  "buildCommand": "cd frontend && npm ci && npm run build && rm -rf node_modules",
  "outputDirectory": "frontend/dist",
  "framework": null,
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
